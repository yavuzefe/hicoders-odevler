export default function OutputFormat(){
    return(
        <div>
            <h3>Output Format</h3>
            <p>For every PAN number listed, print YES if it is valid and NO is it isn't.</p>
        </div>
    )
}